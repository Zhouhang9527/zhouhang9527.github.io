// ===================================================
// ATRI Live2D 自定义配置文件
// ===================================================
// 复制此文件内容到 atri-live2d.njk 的 CONFIG 对象中

const ATRI_CONFIG = {
  // ---------------------------------------------------------------
  // 基础配置
  // ---------------------------------------------------------------
  
  // 模型文件路径（相对于网站根目录）
  modelPath: '/live2d/ATRI/ATRI.model3.json',
  
  // Canvas ID（不建议修改）
  canvasId: 'atri-canvas',
  
  // Canvas 尺寸
  width: 350,        // 宽度（像素）
  height: 500,       // 高度（像素）
  
  // ---------------------------------------------------------------
  // 缩放设置
  // ---------------------------------------------------------------
  
  scale: 0.25,       // 桌面端缩放比例（0.1 - 1.0）
  mobileScale: 0.18, // 移动端缩放比例（0.1 - 1.0）
  
  // ---------------------------------------------------------------
  // 对话消息配置
  // ---------------------------------------------------------------
  
  messages: {
    // 欢迎消息（页面加载时随机显示）
    welcome: [
      '你好呀！我是 ATRI~',
      '欢迎来到 GINKA 的博客！',
      '今天也要加油哦！',
      '很高兴见到你~',
      '有什么我可以帮忙的吗？'
    ],
    
    // 点击消息（点击 ATRI 时随机显示）
    click: [
      '呀！不要戳我~',
      '嘿嘿~',
      '怎么了吗？',
      '在呢在呢~',
      '要和我玩吗？',
      '唔...好痒~'
    ],
    
    // 悬停消息（鼠标悬停时随机显示，30% 概率）
    hover: [
      '有什么需要帮助的吗？',
      '我在这里哦~',
      '要点点我吗？'
    ],
    
    // 复制消息（复制内容时显示）
    copy: [
      '复制成功啦！',
      '内容已经复制好了~',
      '已经帮你复制了！',
      '粘贴的时候小心哦~'
    ],
    
    // 滚动消息（滚动页面时随机显示，10% 概率）
    scroll: [
      '看到什么有趣的内容了吗？',
      '继续往下看吧~',
      '慢慢看，不着急~',
      '这篇文章很有意思呢！'
    ],
    
    // 节假日消息（可选，在特殊日期显示）
    holiday: {
      '01-01': '新年快乐！',
      '02-14': '情人节快乐~',
      '05-01': '劳动节快乐！',
      '10-01': '国庆节快乐！',
      '12-25': '圣诞快乐！',
      // 添加更多节日...
    },
    
    // 时段问候（可选，根据时间显示）
    timeGreeting: {
      morning: '早上好！新的一天开始啦~',    // 6:00 - 12:00
      afternoon: '下午好！要不要休息一下？', // 12:00 - 18:00
      evening: '晚上好！今天过得怎么样？',   // 18:00 - 22:00
      night: '这么晚还不睡吗？要注意身体哦~'  // 22:00 - 6:00
    }
  },
  
  // ---------------------------------------------------------------
  // 动画配置
  // ---------------------------------------------------------------
  
  // 支持的动画组（按优先级排序）
  motionGroups: [
    'tap_body',    // 点击身体
    'shake',       // 摇晃
    'idle',        // 闲置
    'flick_head'   // 摇头
  ],
  
  // 闲置动画间隔（毫秒）
  idleInterval: 15000,  // 15 秒
  
  // ---------------------------------------------------------------
  // 功能开关
  // ---------------------------------------------------------------
  
  enableDrag: true,      // 启用拖拽移动
  enableMessage: true,    // 启用对话消息
  enableControls: true,   // 启用控制面板
  enableMouseTracking: true,  // 启用鼠标跟踪
  
  // ---------------------------------------------------------------
  // 显示设置
  // ---------------------------------------------------------------
  
  messageTimeout: 3000,   // 消息显示时长（毫秒）
  
  // 初始位置
  position: {
    right: 20,   // 距离右边（像素）
    bottom: 0    // 距离底部（像素）
  },
  
  // ---------------------------------------------------------------
  // 高级选项
  // ---------------------------------------------------------------
  
  // 性能设置
  performance: {
    maxFPS: 60,              // 最大帧率
    resolution: 'auto',      // 分辨率（'auto' 或数字）
    antialias: true          // 抗锯齿
  },
  
  // 鼠标跟踪灵敏度
  tracking: {
    angleRange: 30,    // 头部旋转角度范围（度）
    eyeRange: 1,       // 眼球移动范围
    smoothness: 0.5    // 平滑度（0-1）
  },
  
  // 事件概率
  eventProbability: {
    hoverMessage: 0.3,   // 悬停消息概率（0-1）
    scrollMessage: 0.1   // 滚动消息概率（0-1）
  },
  
  // ---------------------------------------------------------------
  // 调试选项
  // ---------------------------------------------------------------
  
  debug: false,  // 启用调试日志
  
  // 日志级别：'none', 'error', 'warn', 'info', 'debug'
  logLevel: 'info'
};

// ---------------------------------------------------------------
// 样式自定义变量
// ---------------------------------------------------------------

const ATRI_STYLES = {
  // 对话框样式
  messageBox: {
    background: 'linear-gradient(135deg, #5b9bd5 0%, #7eb8e6 100%)',
    textColor: '#ffffff',
    borderRadius: '20px',
    padding: '12px 20px',
    fontSize: '14px'
  },
  
  // 控制按钮样式
  controlButton: {
    background: 'linear-gradient(135deg, #5b9bd5 0%, #7eb8e6 100%)',
    hoverScale: 1.1,
    activeScale: 0.95,
    size: 36  // 像素
  },
  
  // 动画时长
  animations: {
    messageSlideIn: '0.3s',
    entrance: '1s',
    hover: '0.3s'
  }
};

// ---------------------------------------------------------------
// 预设配置方案
// ---------------------------------------------------------------

// 方案1: 简约模式（最小功能）
const PRESET_MINIMAL = {
  enableDrag: false,
  enableMessage: false,
  enableControls: false,
  scale: 0.2,
  mobileScale: 0.15
};

// 方案2: 完整模式（所有功能）
const PRESET_FULL = {
  enableDrag: true,
  enableMessage: true,
  enableControls: true,
  scale: 0.3,
  mobileScale: 0.2
};

// 方案3: 移动优化（移动端友好）
const PRESET_MOBILE = {
  enableDrag: false,
  enableMessage: true,
  enableControls: false,
  scale: 0.2,
  mobileScale: 0.15,
  messageTimeout: 2000
};

// 方案4: 性能优先（低配设备）
const PRESET_PERFORMANCE = {
  performance: {
    maxFPS: 30,
    resolution: 1,
    antialias: false
  },
  scale: 0.2,
  mobileScale: 0.12,
  idleInterval: 30000
};

// ---------------------------------------------------------------
// 使用说明
// ---------------------------------------------------------------

/*
1. 基础使用：
   复制 ATRI_CONFIG 到 atri-live2d.njk 的 CONFIG 对象

2. 应用预设：
   const CONFIG = Object.assign({}, ATRI_CONFIG, PRESET_MINIMAL);

3. 自定义消息：
   编辑 messages 对象，添加你想要的消息

4. 调整位置：
   修改 position.right 和 position.bottom

5. 性能优化：
   - 降低 scale 值
   - 减少 maxFPS
   - 增大 idleInterval
   - 设置 resolution: 1

6. 调试模式：
   设置 debug: true 查看详细日志
*/

// ---------------------------------------------------------------
// 导出配置（用于外部引用）
// ---------------------------------------------------------------

if (typeof module !== 'undefined' && module.exports) {
  module.exports = {
    ATRI_CONFIG,
    ATRI_STYLES,
    PRESET_MINIMAL,
    PRESET_FULL,
    PRESET_MOBILE,
    PRESET_PERFORMANCE
  };
}
