<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/aplayer@1.10.1/dist/APlayer.min.css">
<script src="https://cdn.jsdelivr.net/npm/aplayer@1.10.1/dist/APlayer.min.js"></script>

<div id="music-player" class="music-player">
  <div class="music-player-toggle">
    <i class="fa fa-music"></i>
  </div>
  <div class="music-player-container">
    <div class="music-player-header">
      <span class="music-title">音乐播放器</span>
      <button class="music-close">
        <i class="fa fa-times"></i>
      </button>
    </div>
    <div id="aplayer-container"></div>
  </div>
</div>

<style>
.music-player {
  position: fixed;
  top: 100px;
  right: 20px;
  z-index: 10000;
  font-family: 'LXGW WenKai', 'Helvetica Neue', Arial, sans-serif;
  cursor: grab;
}

/* GINKA播放器状态指示器 */
.ginka-player-status {
  position: fixed;
  top: 70px;
  right: 20px;
  z-index: 9999;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 12px;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(15px);
  -webkit-backdrop-filter: blur(15px);
  border: 1px solid rgba(0, 0, 0, 0.05);
  border-radius: 20px;
  font-size: 12px;
  color: #555;
  opacity: 0;
  transform: translateY(-10px) scale(0.95);
  transition: opacity 0.4s ease, transform 0.4s ease;
  pointer-events: none;
}

.ginka-player-status.playing {
  opacity: 1;
  transform: translateY(0) scale(1);
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background-color: #5b99e5;
  transition: background-color 0.3s ease;
}

.ginka-player-status.playing .status-dot {
  background-color: #67C23A;
  animation: pulse 1.5s infinite ease-in-out;
}

@keyframes pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(103, 194, 58, 0.5); }
  50% { box-shadow: 0 0 0 5px rgba(103, 194, 58, 0); }
}

/* 右键菜单样式 */
.ginka-context-menu {
  position: fixed;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  border: 1px solid rgba(0, 0, 0, 0.08);
  border-radius: 10px;
  padding: 6px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
  font-family: 'LXGW WenKai', sans-serif;
  font-size: 14px;
  min-width: 160px;
  z-index: 10001;
}

.ginka-context-menu .menu-item {
  padding: 8px 12px;
  cursor: pointer;
  color: #333;
  border-radius: 6px;
  transition: background-color 0.2s ease, color 0.2s ease;
}

.ginka-context-menu .menu-item:hover {
  background-color: rgba(91, 153, 229, 0.15);
  color: #5b99e5;
}

.ginka-context-menu .menu-separator {
  height: 1px;
  background: rgba(0, 0, 0, 0.08);
  margin: 4px 8px;
}

.music-player-toggle {
  width: 50px;
  height: 50px;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(15px);
  -webkit-backdrop-filter: blur(15px);
  border: 1px solid rgba(0, 0, 0, 0.05);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
}

.music-player-toggle:hover {
  transform: scale(1.1);
  background: rgba(255, 255, 255, 0.95);
  box-shadow: 0 6px 25px rgba(91, 153, 229, 0.2);
}

.music-player-toggle i {
  font-size: 18px;
  color: #5b99e5;
  transition: transform 0.3s ease;
}

.music-player-toggle:hover i {
    transform: rotate(15deg);
}

.music-player-container {
  position: absolute;
  top: 65px;
  right: 0;
  width: 350px;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(0, 0, 0, 0.08);
  border-radius: 16px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
  display: none;
  overflow: hidden;
}

.music-player-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 20px;
  border-bottom: 1px solid rgba(0, 0, 0, 0.05);
}

.music-title {
  font-size: 16px;
  font-weight: 600;
  color: #333;
}

.music-close {
  background: transparent;
  border: none;
  font-size: 16px;
  color: #888;
  cursor: pointer;
  width: 28px;
  height: 28px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.music-close:hover {
  background: rgba(0, 0, 0, 0.08);
  color: #333;
  transform: rotate(90deg);
}

#aplayer-container {
    margin: 0 5px 5px 5px;
}

/* 音乐播放器错误提示样式 */
.music-error {
  text-align: center;
  padding: 30px 20px;
  font-family: 'LXGW WenKai', sans-serif;
}

.error-icon {
  font-size: 48px;
  color: #5b99e5;
  margin-bottom: 15px;
  animation: errorFloat 2.5s ease-in-out infinite;
}

@keyframes errorFloat {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-8px); }
}

.error-text {
  font-size: 16px;
  color: #555;
  margin: 10px 0;
}

.error-subtext {
  font-size: 12px;
  color: #999;
  margin-bottom: 20px;
}

.error-retry-btn {
    background-color: #5b99e5;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 20px;
    cursor: pointer;
    font-family: 'LXGW WenKai', sans-serif;
    font-size: 14px;
    transition: all 0.3s ease;
    box-shadow: 0 2px 10px rgba(91, 153, 229, 0.3);
}

.error-retry-btn:hover {
    background-color: #4a8ad8;
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(91, 153, 229, 0.4);
}

/* 移动端适配 */
@media (max-width: 768px) {
  .music-player {
    top: auto;
    bottom: 20px;
    right: 15px;
  }
  .ginka-player-status {
    top: auto;
    bottom: 80px;
    right: 15px;
  }
  .music-player-container {
    top: auto;
    bottom: 65px;
    right: -15px; /* 修正位置 */
    width: calc(100vw - 20px);
    max-width: 350px;
  }
}
</style>
