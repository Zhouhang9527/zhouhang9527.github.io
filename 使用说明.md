## 🚀 快速开始

### 方法1: 双击运行(推荐)
1. 双击 `deploy.bat` - 启动完整部署流程
2. 或双击 `build.ps1` - 仅构建不启动服务器

### 方法2: PowerShell 命令
```powershell
# 完整部署(清理+生成+启动服务器)
.\deploy.ps1

# 仅构建(清理+生成)
.\build.ps1
```

### 方法3: 传统命令
```powershell
hexo clean && hexo generate && hexo server
```

---

## 📝 脚本说明

| 脚本 | 功能 | 何时使用 |
|------|------|----------|
| `deploy.ps1` | 清理+生成+启动服务器 | 日常开发预览 |
| `deploy.bat` | 同上(批处理版本) | 双击运行更方便 |
| `build.ps1` | 仅清理+生成 | 准备部署前构建 |

---

## ⚠️ 常见问题

**Q: PowerShell 脚本无法运行?**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**Q: 中文显示乱码?**  
脚本已自动处理 UTF-8 编码,如仍有问题运行:
```powershell
chcp 65001
```

**Q: 如何停止服务器?**  
按 `Ctrl + C`

---

## 📚 详细文档

查看 `DEPLOY_README.md` 了解完整使用说明和技术细节。

---

访问: **http://localhost:4000** 查看博客
