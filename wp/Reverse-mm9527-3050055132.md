姓名:周航
学号:202531050763
qq:3050055132
NSS平台昵称:mm9527
### 题目一:一种很新的签到
先拖进die,发现没有壳,拖进ida打开(后面的题除了有壳的就省略了)
![[Pasted image 20251103232546.png]]
f5看伪代码![[Pasted image 20251103232747.png]]
发现意义不明,shift f12查看字符串,找了一下发现flag直接给出来了
![[Pasted image 20251103232848.png]]
### 题目二:fl~ow~er~
依旧ida,因为有花指令f5不了了,找一下有哪些花指令![[Pasted image 20251103233119.png]]![[Pasted image 20251103233135.png]]
两种比较常见的花指令,基本按一下u就知道怎么改了,直接把被跳过的字节ctrl+n nop掉,再按c让剩下的变成代码,发现已经恢复正常了![[Pasted image 20251103233312.png]]
![[Pasted image 20251103233447.png]]
jz+jnz同理,必定会跳转就把一个改成jmp再nop掉另一个
![[Pasted image 20251103233645.png]]![[Pasted image 20251103233707.png]]
这个时候发现还是不能f5,发现有一个比较隐蔽的花指令,异或两个相同的寄存器,接一个jz,也是必定跳转,改成jmp,再nop多余字节![[Pasted image 20251103233957.png]]
改完这个,再对着main先按u取消定义再按p定义函数,发现可以正常f5了![[Pasted image 20251103234255.png]]
双击进入loc_401000函数内部,发现也有花指令和上面的一样,去掉花指令后f5看伪代码,发现只是一个异或![[Pasted image 20251103234501.png]]
再提取密文写一个简单异或脚本解出flag
![[Pasted image 20251104133824.png]]
![[Pasted image 20251104133841.png]]
### 题目三:哈基米
附件是一个exe,用ide打开,发现是用PyInstaller打包的![[Pasted image 20251104110246.png]]
直接拖进![[Pasted image 20251104110649.png]]
得到一堆pyc文件,看到python版本是3.8![[Pasted image 20251104110755.png]]
python3.9版本以上（包含3.9）用pycdc反编译
python3.9版本以下（不包含3.9）可以用https://github.com/one066/Exe-decompiling,也可以用uncompyle6,最开始没注意版本,一直做不出来...
先反编译HJM.pyc
![[Pasted image 20251104113113.png]]
可以看到这段代码引用了另一个文件里面的字符串CHARS1
再去找到这个文件,反编译得到
![[Pasted image 20251104113428.png]]
写解密脚本,能直接照搬生成CHARS1的代码,运行得到flag![[Pasted image 20251104133525.png]]
![[Pasted image 20251104133546.png]]
### 题目四:Ultimate Packer for eXecutables
die查壳,可以看到是upx,
![[Pasted image 20251104182310.png]]
但是直接用upx去壳是去不掉的![[Pasted image 20251104182334.png]]
die已经识别出了文件头被修改了,我们需要把UPX头改回来,用010EDitor打开![[Pasted image 20251104182708.png]]
也是可以看到UPX头被修改了,我们需要手动改回来,主意最好用覆盖修改,不然容易改错(不知道怎么改的可以先给一个程序加壳,再查看文件头对照修改)
![[Pasted image 20251104183017.png]]
改好之后保存,再用upx -d去壳
![[Pasted image 20251104183119.png]]
之后就能拖进ida静态分析了![[Pasted image 20251104183221.png]]
写解密脚本运行得到flag,注意这里直接按照加密逻辑倒着写是得不到flag的,因为input×122,会导致数值大于一个2的八次方(一个字节占八位,2**8=256),会导致溢出,简单的写法就是遍历可打印字符的ascii码范围:
![[Pasted image 20251104184608.png]]![[Pasted image 20251104184616.png]]
### 题目五:debuggggg
用ida打开,字符串+交叉引用定位主逻辑/直接进main函数
![[Pasted image 20251104221613.png]]![[Pasted image 20251104221751.png]]
因为这里用了rand()函数,而不知道seed我们是无法还原buf数组的,找一下发现找不到seed,由题目名可以知道这道题需要我们动态调试,但这是linux操作系统的文件,这里选择远程连接wsl动态调试![[Pasted image 20251104222157.png]]![[Pasted image 20251104222303.png]]

断点下在sub_55555555526B(s:s,XLVGSD_mfbuha_sstgpr_9d_acc_wbi_:XLVGSD_mfbuha_sstgpr_9d_acc_wbi_);;f9启动调试得到buf数组的内容,???发现程序运行不到断点就结束了,因为在这之前有个输入长度检测,检查长度不符合就终止程序![[Pasted image 20251104223306.png]]
可以通过patch修改这里的跳转为jnz或者jmp,就可以无视这个长度检验了![[Pasted image 20251104223024.png]]
![[Pasted image 20251104223356.png]]
![[Pasted image 20251104223512.png]]注意改完之后重启一下ida,f启动调试,这里要选copy new,不然的话在wsl上运行的还是打补丁之前的程序,你会发现还是不能命中断点(血的教训🥲)![[Pasted image 20251104224152.png]]
![[Pasted image 20251104224419.png]]
提取到利用rand函数生成的这串数据就可以写解密脚本了,在模运算中:
如果加密是 C=(P+K)(modM)
那么解密是P=(C−K+M)(modM)
如果加密是C=(P−K)(modM)
那么解密是P=(C+K)(modM)
![[Pasted image 20251105000643.png]]
![[Pasted image 20251105000710.png]]
### 题目六:CodeRunner
Unity3D题,Unity有Mono和IL2CPP两种不同的脚本后端,解压附件可以知道这道题是IL2CPP![[Pasted image 20251105125646.png]]
我们需要用IL2cppdumper来从Unity il2cpp的二进制文件中获取类型、方法(反编译)可以得到![[Pasted image 20251105125812.png]]
但是这道题直接反编译会报错,把GameAssembly.dll拖进ide会发现文件被UPX加壳了,并且文件头被修改,直接拖进010 Editor
![[Pasted image 20251105130503.png]]
![[Pasted image 20251105131649.png]]
直接把NSS全换成UPX再报错就能用UPX去壳了,现在就可以用IL2cppdumper反编译了
![[Pasted image 20251105130714.png]]用dnSpy反编译![[Pasted image 20251105130959.png]]
再用ida反编译GameAssembly文件,注意这里需要用到il2cppdumper反编译出来的.py .script .h文件(大概是给函数命名?)![[Pasted image 20251105132137.png]]
依次选择,然后等待🤷‍♂️![[Pasted image 20251105132156.png]]![[Pasted image 20251105132204.png]]![[Pasted image 20251105132214.png]]
然后搜索刚刚在dnSpy里面看到的二维码创建函数所在的类名QRCodeBuilder![[Pasted image 20251105134410.png]]
![[Pasted image 20251105140316.png]]
awake函数调用了construct函数,分析一下,推测应该是根据qrmatrix来动态生成二维码(qr为二维码,matrix为矩阵,所以我们需要动调获取它的值![[Pasted image 20251105141018.png]]
![[Pasted image 20251105141113.png]]但是直接动态调试会崩溃,发现先调用了一个叫update的函数分析发现内部有反调试函数WinAPI__DetectDebugger,直接将这个函数的第一行patch为ret,发现成功绕过反调试,我们需要先获取this指针的值(x64 架构下：为RCX 寄存器的值
在 x32 架构下：为ECX 寄存器的值)断点下在函数入口,这里我们只能用![[Pasted image 20251105142303.png]],因为我们反编译的只是一个文件,需要先启动游戏,再附加到进程
rcx:0x23453F87F60
再去dnSpy找到qrMatrix FieldOffset(Offset = "0x48"),这个0x48就是他的偏移量,动调时他的地址就在this(rcx)+Offset=23453F87FA8,按g跳转到这个地址![[Pasted image 20251105143407.png]]
提取到23453F8A0,这就是qxMatrix内容所在的地址,再次按g跳转提取一大串1111100011111,
网上找现成的转二维码脚本![[Pasted image 20251105152619.png]]
得到![E:\python\final_qr_code.png](file:///e%3A/python/final_qr_code.png)
### 题目七:来了嗷，常见加密大杂烩！
ida打开就能看到主逻辑,这道题就考的是写脚本![[Pasted image 20251105152923.png]]
一个函数一个函数单独写好写一些,解不出来就动态调试一步一步对照(base64直接用的现成的)
def base64_decode(s, base64chars):

    lookup = {char: index for index, char in enumerate(base64chars)}

    s = s.rstrip('=')

    bit_string = ''

    for char in s:

        if char in lookup:

            bit_string += format(lookup[char], '06b')

    decoded_bytes = bytearray()

    for i in range(0, len(bit_string), 8):

        byte_slice = bit_string[i:i+8]

        if len(byte_slice) == 8:

            decoded_bytes.append(int(byte_slice, 2))

    return bytes(decoded_bytes)

  

basetable = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789+/'

enc = "2FDjeKsMelmcoyk4AbBvVNXlgv5TRGAWT+AKXHjHn4m="

a1 = base64_decode(enc, basetable)

  

def int8(x): return x % 256

  

def rc4_init(key, lenkey):

    key = [ord(i) for i in key]

    a = list(range(256))

    v5 = 0

    for j in range(256):

        v5 = int8(a[j] + v5 + key[j % lenkey])

        v4 = a[j]

        a[j] = a[v5]

        a[v5] = v4

    return a

  

def rc4_crypt(num, key, lenkey):

    s = rc4_init(key, lenkey)

    num = list(num)

    v9, v8 = 0, 0

    for i in range(len(num)):

        v9 += 1

        v8 = int8(s[v9] + v8)

        v6 = s[v9]

        s[v9] = s[v8]

        s[v8] = v6

        num[i] ^= s[int8(s[v9] + s[v8])]

    return num

  

def xtea(v0, v1, key):

    delta = 4412486

    total = (delta * 32) & 0xFFFFFFFF

    for _ in range(32):

        v1 = (v1 - ((((v0 >> 5) ^ ((v0 << 4) & 0xFFFFFFFF)) + v0)

                   ^ ((key[(total >> 11) & 3] + total) & 0xFFFFFFFF))) & 0xFFFFFFFF

        total = (total - delta) & 0xFFFFFFFF

        v0 = (v0 - ((((v1 >> 5) ^ ((v1 << 4) & 0xFFFFFFFF)) + v1)

                   ^ ((key[total & 3] + total) & 0xFFFFFFFF))) & 0xFFFFFFFF

    return v0, v1

  

def tea(a1, a2, key):

    delta = 5133139

    sum_ = (32 * delta) % (2**32)

    for _ in range(32):

        a2_ = (a2 - (((a1 + sum_) % (2**32)) ^ ((16 * a1 + key[2]) % (2**32)) ^ ((a1 >> 5) + key[3]))) % (2**32)

        a1_ = (a1 - (((a2_ + sum_) % (2**32)) ^ ((16 * a2_ + key[0]) % (2**32)) ^ ((a2_ >> 5) + key[1]))) % (2**32)

        sum_ = (sum_ - delta) % (2**32)

        a1, a2 = a1_, a2_

    return a1, a2

  

a2 = rc4_crypt(a1, "CreamBread", len("CreamBread"))

a2b = bytes(a2)

  
  

f = []

for i in range(0, 32, 4):

    f.append(int.from_bytes(a2b[i:i+4], 'little'))

xtea_key = [0x63736373, 0x00796369, 0x656b6954, 0x73696552]

tea_key = [0x79756173, 0x79797379, 0x6b63617a, 0x69796568]

  

a3 = []

for i in range(0, 8, 2):

    a, b = xtea(f[i], f[i+1], xtea_key)

    a, b = tea(a, b, tea_key)

    a3.append(a)

    a3.append(b)

  

final_bytes = []

for val in a3:

    final_bytes.extend([

        val & 0xFF,

        (val >> 8) & 0xFF,

        (val >> 16) & 0xFF,

        (val >> 24) & 0xFF

    ])

  

print(final_bytes)

print(''.join(chr(i) for i in final_bytes))
### 题目八:ez_tea
考点为反反调试,可以通过找一些系统api来定位反调试函数(参考[反调试技术常用API，用来对付检测od和自动退出程序-腾讯云开发者社区-腾讯云](https://cloud.tencent.com/developer/article/2348105))
这道题由于每次反调试终止程序之前都会有一个对话框提示,所以也可以通过查找Messagebox的引用来定位,绕过反调试之后动态调试发现输入的flag被混淆了,tea加密的key也被替换了
写脚本逆向这两个操作
`from ctypes import c_uint32`

`import struct`

`enc = [0xddeb1ce8, 0x46696691, 0x47baf5fd, 0x73b625ed, 0xfd3dc499, 0x19e7efb, 0x9bf87340, 0xf28b48b4, 0xc07fbe5a, 0xd240be6e, 0xb091ff6b, 0x7d0f0bf8]`

`key = [0x36433331, 0x45344537, 0x33314534, 0x32373137]`

`delta=218105633`

`flag=[]`

`for i in range(0, len(enc), 2):`

    `a1 = c_uint32(enc[i])`

    `a2 = c_uint32(enc[i+1])`

    `sum_val = c_uint32(delta * 32)`

    `for j in range(32):`

        `a2.value -= (a1.value >> 5) ^ (key[((sum_val.value >> 11) ^ 1) & 3] + 16 * a1.value)`

        `a1.value -= (a2.value >> 5) ^ (key[(sum_val.value >> 11) & 3] + 16 * a2.value)`

        `sum_val.value -= delta`

    `flag.append(int(hex(a1.value),16))`

    `flag.append(int(hex(a2.value),16))`

`s=b''`

`for i in flag:`

    `s+=struct.pack('<I',i)`

`s=s.decode('ascii',errors='ignore')`

`s=list(s)`

`print(len(s))`

`for i in range(len(s)):`

    `s[i]=ord(s[i])`

`for j in range(0,22):`

        `s[2*j+1]^=2`

        `s[2*j]^=0x72`

        `s[2*j+1]+=2`

        `s[2*j]-=1`

`for i in s:`

    `print(chr(i),end='')`
    ==运行得到flag==